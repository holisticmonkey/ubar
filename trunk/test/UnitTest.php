<?
// figure out how to run any function that starts with 'test' in the unit test
// figure out how to report errors without failing
// make sure tests accumulate errors, warnings, info, and stats on each test suite name (from file minus ".php") and test name (method name, god knows how)
class UnitTest {

	function assertTrue($condition) {
		if($condition === true) {
			// passes
		} else {
			// fails
		}
	}
	
	function assertFalse($condition) {
		// place-holder
	}
}
?>